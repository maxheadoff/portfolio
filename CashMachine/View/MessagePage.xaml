<Page x:Class="CashMachine.MessagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="MessagePage" Style="{StaticResource PageStyle}">
    <Page.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Background" Value="Aquamarine"/>
            <Setter Property="Foreground" Value="Green"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
        </Style>
    </Page.Resources>
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Информация" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Top" />
        <Label Name="lbTitle" Grid.Row="0"  Margin="20" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
        <TextBox Name="tbMessage" Margin="20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Grid.Row="1" TextAlignment="Center"/>
    </Grid>
</Page>
